language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
branches:
  only:
  - master
  - develop
services:
  - postgresql
before_script:
  - psql -c 'create database elms;' -U postgres
script:
  - pytest