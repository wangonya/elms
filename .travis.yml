language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
branches:
  only:
  - master
  - develop
ActiveRecord::Base.establish_connection :adapter => 'sqlite3',
                                        :database => ':memory:'
script:
  - pytest